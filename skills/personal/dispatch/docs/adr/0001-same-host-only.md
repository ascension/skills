# Unnamed dispatch stays on this host

With no adapter on the intent, dispatch stays on the host this session is already on. Herdr rolls to Herdr. An explicit adapter (user or spawn intent) is a **pin** and is the allowed override. Accidental crossing is still `/handoff` plus the user starting the other host.

**Status:** accepted
