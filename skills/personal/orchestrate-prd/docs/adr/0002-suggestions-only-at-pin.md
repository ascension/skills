# Model suggestions only at pin

The coordinator may propose a model override as a **suggestion**, but only while pinning the control plane. After the relay opens, a missing override means the **role** default plus **effort** — no mid-run ask. Mid-run approval would break AFK.

**Status:** accepted
